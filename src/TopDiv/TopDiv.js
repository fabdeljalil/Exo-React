import React from "react";
import Button from "../Button/Button";

const TopDiv = () => {
  return (
    <div>
      <Button name="N.W"></Button>
      <Button name="H"></Button>
      <Button name="N.E"></Button>
    </div>
  );
};

export default TopDiv;
